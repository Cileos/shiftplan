#!/bin/bash

. script/_lib

if [ $# -gt 0 ]
then
  ret=$((0))

  exp=2
  for flavor in "$@"
  do
    time run_suite $flavor || ret=$(($ret+$exp))
    exp=$(($exp*2))
  done

  header "Done (exit code: $ret)"
  exit $ret
else
  run_suite "all"
fi

