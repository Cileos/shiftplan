= simple_form_for copy_week,
  url: nested_resources_for(copy_week.plan) + [:copy_week] do |f|
  .inputs
    = f.input :source_string, as: :stringy_week
    = f.input :target_string, as: :stringy_week
  .actions
    = f.responsive_submit_button :copy
