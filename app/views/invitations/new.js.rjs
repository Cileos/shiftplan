page.decorate resource do |resource|
  resource.append_modal_with_form(
    header: content_tag(:h3,
      t(:'invitations.new.invite_employee',
        employee_name: resource.employee.name
      )
    )
  )
end
