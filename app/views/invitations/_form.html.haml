= simple_form_for invitation, url: [current_account, current_organization, invitation], remote: true do |f|
  = f.error_notification

  .inputs
    = f.input :employee_id, as: :hidden
    = f.input :email, autofocus: true

  .actions
    = f.responsive_submit_button :send_invitation
