page.decorate @invitation do |invitation|
  invitation.append_modal_with_form(
    header: content_tag(:h3,
      t(:'invitations.new.invite_employee',
        employee_name: invitation.employee.name
      )
    )
  )
end
