- title filter.caption, false

-# We try to copy google calendar's layout: Basically, they use a table for the
-# columns, but only one row for the content itself, because schedulings can span
-# over multiple hours.
-#
-# Downside: we have to set the height of "rows" and schedulings per css
-#      see _vertical_bars.css.scss

%table#calendar.hours-in-week.table.table-striped.table-bordered.table-condensed{class: filter.range, data: filter.table_metadata}
  %thead
    %tr
      %th
        = render 'week_switch', filter: filter
      - filter.formatted_days.each do |full, abbr|
        %th= abbr_tag(abbr, full, class: 'day_name')
  %tbody
    %tr
      %th.hours
        - filter.working_hours.each do |hour|
          %div= hour
      - filter.days.each do |day|
        %td.day{data: filter.cell_metadata(day)}
          = filter.cell_content(day)

