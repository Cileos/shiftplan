- unless schedulings.empty?
  %ul
    - schedulings.map(&:decorate).each do |s|
      %li{class: s.team_class, data: s.metadata}
        - if s.team
          %abbr{title: s.long}= s.short
        - else
          %span= s.short
