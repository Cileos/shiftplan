- unless schedulings.empty?
  %ul
    - schedulings.map(&:decorate).each do |s|
      %li{class: s.css_class, data: s.metadata}
        - if s.team
          %abbr{title: s.long, rel: "tooltip"}= s.short
        - else
          %span= s.short
