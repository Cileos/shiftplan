page.decorate filter do |f|

  f.append_modal body: ''

  f.records.each do |scheduling|
    f.append_to_modal render('tabs', scheduling: scheduling)
  end


  # TODO append the new form
  #filter.append_to_modal render('new_form', scheduling: Scheduling.new)
end
