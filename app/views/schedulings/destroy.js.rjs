page.decorate resource.concurrent do |filter|
  filter.update_wwt_diff_for(resource.employee)
  filter.update_cell_for(resource)
  filter.remove :form_for, resource
  page['scheduling_new_quickie'].val(resource.quickie)
end

