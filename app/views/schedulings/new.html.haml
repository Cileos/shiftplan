= simple_form_for resource, :url => plan_schedulings_path(resource.plan) do |f|
  = f.association :employee, :as => :select, :include_blank => false
  = f.input :day, :collection => resource.plan.days, :include_blank => false
  = f.input :quicky
  = f.button :submit, translate_action('.create')