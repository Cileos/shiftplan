{
  'flash': {
    'notice': '<%= escape_javascript(flash[:notice]) %>'
  },
  'qualification': <%= @qualification.to_json %>,
  'html': {
    'append': {
      '#qualifications': '<%= escape_javascript(render(:partial => 'qualification', :object => @qualification)) %>'
    }
  }
}