{
  'flash': {
    'notice': '<%= escape_javascript(flash[:notice]) %>'
  },
  'qualification': <%= @qualification.to_json.html_safe %>,
  'html': {
    'append': {
      '#list': '<%= escape_javascript(render(:partial => 'qualification', :object => @qualification)) %>'
    }
  }
}