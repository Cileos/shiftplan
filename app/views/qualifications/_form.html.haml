= simple_form_for [current_account, current_organization, qualification],
  html: { class: 'form-horizontal' } do |f|
  .inputs.edit-team
    = f.input :name, autofocus: true
  .actions
    = f.responsive_submit_button do
      = ta create_or_update
