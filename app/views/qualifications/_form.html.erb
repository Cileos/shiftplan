<h3><%= t(:new_qualification) %></h3>

<% form_for Qualification.new do |f| %>
  <fieldset>
  	<fieldset>
  	  <div>
  	    <%= f.label :name %>
  	    <%= f.text_field :name %>
  	  </div>
    </fieldset>
  	<%= f.submit t(:save) %>
  </fieldset>
<% end %>