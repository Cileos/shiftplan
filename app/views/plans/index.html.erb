<ol>
  <% @plans.each do |plan| -%>
    <% content_tag_for(:li, plan) do %>
			<%= link_to plan_name_and_dates(plan), plan_path(plan) %>
		<% end %>
  <% end -%>
</ol>

<%= render :partial => 'form' %>