<%= content_tag(:tr, :class => dom_id(employee)) do -%>
  <td><input type="checkbox" /></td>
  <td><%= image_tag(employee.gravatar_url(:size => 30)) %></td>
	<td><%= employee.full_name %></td>
	<td><%= employee.possible_workplaces.map(&:name).join(', ') %></td>
<% end -%>
