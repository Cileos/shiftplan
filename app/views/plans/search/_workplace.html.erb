<%= content_tag(:tr, :class => dom_id(workplace)) do -%>
  <td><input type="checkbox" /></td>
	<td><%= workplace.name %></td>
	<td><%= workplace.cached_tag_list.blank? ? '' : truncate(workplace.cached_tag_list) %></td>
<% end -%>
