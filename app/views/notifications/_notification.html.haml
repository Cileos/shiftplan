%li{id: dom_id(notification) }
  %a
    %subject.subject
      = notification.subject
    %blurb.blurb
      = notification.blurb
    %actions.actions
      = link_to :mark_as_read, read_notification_path(notification),
        method: :put, remote: true, remote: true
