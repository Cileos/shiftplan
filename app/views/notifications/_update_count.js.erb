<% if (count = current_user.new_notifications_count) > 0 %>
  $("li#notification-hub").addClass('has_new');
  $('a#notifications-count .count').text(<%= count %>);
<% else %>
  $("li#notification-hub").removeClass('has_new');
  $('a#notifications-count .count').text('');
<% end %>
