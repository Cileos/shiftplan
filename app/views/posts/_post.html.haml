.meta= t(:'posts.meta_data', author: post.author.name, published_at: l(post.published_at, format: :tiny))
%h3= post.title
- if truncation
  .body= truncate(post.body, :length => 300, :omission => ' …')
- else
  .body= post.body
