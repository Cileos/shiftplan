= simple_form_for [current_account, current_organization, resource],
  html: { class: 'form-horizontal' }, remote: true do |f|
  .inputs.edit-team
    = f.input :name
    = f.input :shortcut
    = f.input :color, :class => 'miniColors'
  .actions
    = f.create_or_update_button
