- title resource.name

= simple_form_for [current_account, current_organization, resource] do |f|
  = f.input :name
  = f.input :shortcut
  = f.input :color
  = f.button :submit, class: 'btn btn-primary'
