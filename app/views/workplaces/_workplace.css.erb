.plan .<%= dom_id(workplace) %> .shift,
.<%= dom_id(workplace) %> div {
	background-color: <%= workplace.color %>;
}
#workplace_<%= workplace.id %> div.color {
	background-color: <%= workplace.color %>;
}
