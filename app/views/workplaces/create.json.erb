{
  'flash': {
    'notice': '<%= escape_javascript(flash[:notice]) %>'
  },
  'workplace': <%=raw @workplace.to_json %>,
  'html': {
    'append': {
      '#list': '<%= escape_javascript(render(:partial => 'workplace', :object => @workplace)) %>'
    }
  }
}