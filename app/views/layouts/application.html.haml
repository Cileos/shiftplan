!!!5
%html
  = render 'layouts/head'
  %body
    #page
      = render 'layouts/top'
      %section{:role => 'content'}
        = render 'flash'
        - if show_title?
          %h2#title= yield(:title)

        = yield
      = render 'layouts/footer'
