!!!5
%html
  = render 'layouts/head'
  %body
    = render 'layouts/top'
    
    #page
      %section{:role => 'content'}
        = render 'flash'
        - if show_title?
          %h2#title= yield(:title)

        = yield
      = render 'layouts/footer'
