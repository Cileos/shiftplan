<% content_tag(:td, :id => "#{dom_id(employee)}_day_#{day_of_week}") do %>
  <ul class="statuses">
    <%= render :partial => 'default_statuses/default_status.html', :collection => statuses, :locals => { :employee => employee } %>
  </ul>
<% end %>
