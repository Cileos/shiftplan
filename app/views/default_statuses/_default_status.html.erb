<% content_tag_for(:li, default_status, :class => "#{default_status.status.to_s.underscore} resource", :'data-form-values' => default_status.form_values_json) do %>
  <%= link_to t(:status_from_to, :from => l(default_status.start_time, :format => :time), :to => l(default_status.end_time, :format => :time)),
    default_status.new_record? ?
      new_employee_default_status_path(employee, :status => default_status.attributes.slice('start_time', 'end_time', 'status', 'day_of_week', 'employee_id')) :
      edit_employee_default_status_path(employee, default_status), :class => 'edit' %>
<% end %>
