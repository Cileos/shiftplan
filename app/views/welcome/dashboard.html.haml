- title :dashboard

- for_header do
  = link_to '.employees', employees_path
  = link_to '.new_plan', new_plan_path

%h3= ta :plans
%ul.plans
  - current_user.organization.plans.each do |plan|
    %li= link_to plan.name, plan

- for_footer :class => 'ui-bar' do
  = render 'session'
