- title :dashboard

-# comment in again, when we are able to support multiple accounts/billing
-# - if can? :create, Account.new(user_id: current_user.id)
-#   - masterhead_toolbar do
-#     = link_to ta(:add_account),
-#       new_account_path(account: { user_id: current_user.id }),
-#       class: 'button button-add', title: ta(:add_account)

.row.two-columns.inverted.dashboard
  = render 'schedulings/for_dashboard', schedulings: @upcoming
  = render 'notifications/for_dashboard', notifications: @notifications
  = render 'posts/for_dashboard', posts: @posts
