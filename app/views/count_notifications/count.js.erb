$("a#notifications-count").replaceAndRefreshWith('<%= j render("notifications/count", count: @count) %>');
<% if @count > 0 %>
  $("li#notification-hub").addClass('has_new');
<% else %>
  $("li#notification-hub").removeClass('has_new');
<% end %>
