- unless controller.flash.blank?
  #flash
    - controller.flash.to_hash.with_indifferent_access.slice(:info, :alert, :notice, :error).each do |name, msg|
      .flash.alert{class: "alert-#{name}"}
        %a.close{'data-dismiss' => 'alert'} ×
        = msg
