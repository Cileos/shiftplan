{{view Shiftplan.DoableDoneView checkedBinding="done"}}
<a class="name" {{action editMilestone this href=true}}>{{name}}</a>
{{#if due_at}}
<span class="due_on">{{formatted_due_on}}</span>
{{/if}}
{{#if tasksEnabled}}
  {{view Shiftplan.TaskListView contentBinding="savedTasks"}}
  <a {{action newTask this href=true}} data-icon="&#xf067;">neue Aufgabe</a>
{{/if}}
