<!-- @tatze you can replace all tags wit proper ones, keep the classes plz -->
{{view Shiftplan.MilestoneDoneView checkedBinding="done"}}
<h4 class="name">{{name}}</h4>
{{#if due_at}}
<span class="due_on">{{formatted_due_on}}</span>
{{/if}}
<div class="description">Just a fake description. It may be very long, so we might shorten it.</div>
<span class="employee_name">Pur Doohd<span>
{{#unless isValid}}
<div class="error">{{errorMessages}}</div>
{{/unless}}
<a {{action editMilestone this}} class="button button-small" href="#">Bearbeiten</a>
