{{#unless isValid}}
<div>{{errorMessages}}</div>
{{/unless}}
{{view Shiftplan.Fields.Name valueBinding="name"}}
{{view Shiftplan.Fields.DueDateField valueBinding="formatted_due_on"}}
<button {{action save}}>{{view.buttonLabel}}</button>

