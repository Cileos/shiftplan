# jQuery(document).ready ->
  # return if $('#legend').length == 0

  # $('#legend').dialog({autoOpen: false, minWidth: 500, modal: false})

  # $('#display_legend').click -> $('#legend').dialog('open')
